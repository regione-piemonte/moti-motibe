InterventoReferente:
  type: object
  properties:
    id:
      type: integer
    intervento:
      type: object
      $ref: '#/definitions/Intervento'
    utente:
      type: object
      $ref: '#/definitions/Utente'
